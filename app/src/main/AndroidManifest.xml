<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.iu.ckresnye.cassiesuselessfunfacts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".TMNTQuiz"
            android:label="@string/title_activity_tmntquiz"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".SewingQuiz"
            android:label="@string/title_activity_sewing_quiz"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".WhalesQuiz"
            android:label="@string/title_activity_whales_quiz"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>

</manifest>